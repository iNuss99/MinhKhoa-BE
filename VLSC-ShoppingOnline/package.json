{
  "name": "vlsc-shopping-online",
  "version": "1.0.0",
  "description": "Root configuration to run MERN Shopping Online services simultaneously",
  "workspaces": [
    "server",
    "client-admin",
    "client-customer"
  ],
  "scripts": {
    "start": "concurrently -n \"SERVER,ADMIN,CUSTOMER\" -c \"blue,green,magenta\" \"npm --prefix server start\" \"npm --prefix client-admin start\" \"npm --prefix client-customer start\"",
    "build": "npm --prefix client-admin run build && npm --prefix client-customer run build && node scripts/copy-admin.js",
    "install:all": "npm install && npm --prefix server install && npm --prefix client-admin install && npm --prefix client-customer install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
